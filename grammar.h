#ifndef GRAMMAR
#define GRAMMAR

/********grammar info********/

#include "def.h"

#define MAX_GRAMMAR_N 64

int grammar_n;

int terminal_n;

int grammar_length[MAX_GRAMMAR_N];

int grammar_index[MAX_GRAMMAR_N];


#endif